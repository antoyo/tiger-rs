let type intArray = array of int
    var len := 12
    var a := intArray[len] of 0
    var b := intArray[12] of 7
    var value := a[0]
in (
    printi(value);
    a[0] := 10;
    printi(a[0]);
    for i := 0 to len - 1 do
        a[i] := i;
    for i := 0 to len - 1 do
        printi(a[i])
)
end
