(
    let var i := 0
    in
        while i < 10 do (
            printi(i);
            break;
            i := i + 1
        )
    end;

    let var i := 0
    in
        while i < 10 do (
            printi(i);
            if i = 5 then
                break;
            i := i + 1
        )
    end;

    let var i := 0
        var iter_end := 10
    in
        if i < iter_end then
            while 1 do (
                printi(i);
                if i < iter_end then
                    i := i + 1
                else
                    break
            )
    end;

    for i := 0 to 10 do
        printi(i)
)
